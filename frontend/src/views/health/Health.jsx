import React from 'react';

const Health = () => {
    return (
        <div>
            <h1>Health check</h1>
            <p>Frontend is running!</p>
        </div>
    );
};

export default Health;